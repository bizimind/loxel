/** Small red dot indicator for unread terminal notifications. */
export function NotificationDot() {
  return (
    <span className="bg-destructive absolute -top-0.5 -right-0.5 size-2.5 rounded-full border border-white shadow-sm" />
  );
}
