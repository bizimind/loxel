{
  "name": "@bizimind/cc-tool-guard",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "bun build --compile src/tool-guard.ts --outfile dist/cc-tool-guard",
    "test": "bun test",
    "typecheck": "tsgo --noEmit"
  }
}
